{% extends 'base.html.twig' %}

{% block title %}Résultat du test{% endblock %}

{% block body %}
<div class="max-w-xl mx-auto px-6 py-24 text-center">

    {% set passed = application.testScore >= application.assignedTest.minScore %}

    <div class="bg-white rounded-3xl shadow-2xl p-10">
        <h1 class="text-5xl font-black mb-6
            {{ passed ? 'text-green-600' : 'text-red-600' }}">
            {{ passed ? '🎉 Félicitations' : '❌ Test échoué' }}
        </h1>

        <p class="text-2xl mb-6">
            Score : <strong>{{ application.testScore|round }}%</strong>
        </p>

        <p class="text-gray-600 mb-10">
            {{ passed ? 'Votre candidature continue.' : 'Merci pour votre participation.' }}
        </p>

        <a href="{{ path('candidate_applications_list') }}"
           class="inline-block px-10 py-4 rounded-xl font-bold text-white
                  bg-gradient-to-r from-purple-600 to-pink-600 hover:shadow-xl transition">
            Retour aux candidatures
        </a>
    </div>

</div>
{% endblock %}
