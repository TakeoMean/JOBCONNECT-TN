{# templates/recruiter/dashboard.html.twig #}
{% extends 'base.html.twig' %}

{% block body %}
<div class="min-h-screen bg-gradient-to-br from-purple-50 to-pink-50 py-10">
    <div class="text-center mb-10">
        <h1 class="text-3xl md:text-4xl font-black">
            Bonjour {{ app.user.companyName }} !
        </h1>

        <p class="text-lg mt-3">
            Abonnement :
            {% if app.user.subscription %}
                <span class="bg-purple-500 text-white px-3 py-1 rounded-full font-bold text-sm">
                    {{ app.user.subscription|upper }}
                </span>
                <span class="text-sm text-gray-600 ml-2">
                    (expire le {{ app.user.subscriptionEndsAt|date('d/m/Y') }})
                </span>
            {% else %}
                <span class="bg-gray-300 px-3 py-1 rounded-full font-bold text-sm">
                    GRATUIT
                </span>
            {% endif %}
        </p>

        {% if app.user.logo %}
            <img src="{{ asset('uploads/recruiter/logo/' ~ app.user.logo) }}"
                 class="mx-auto mt-4 w-32 h-32 object-cover rounded-xl">
        {% endif %}

        {% if app.user.photo %}
            <img src="{{ asset('uploads/user/photo/' ~ app.user.photo) }}"
                 class="mx-auto mt-4 w-24 h-24 object-cover rounded-full">
        {% endif %}
    </div>

    <div class="grid md:grid-cols-3 gap-6 max-w-6xl mx-auto">
        <div class="bg-white rounded-2xl shadow-lg p-6 text-center">
            <div class="text-5xl mb-3">📄</div>
            <p class="text-3xl font-black text-blue-600">12</p>
            <p class="text-sm mt-1">Offres publiées</p>
        </div>

        <div class="bg-white rounded-2xl shadow-lg p-6 text-center">
            <div class="text-5xl mb-3">💌</div>
            <p class="text-3xl font-black text-red-600">5</p>
            <p class="text-sm mt-1">Nouvelles candidatures</p>
        </div>

        <div class="bg-white rounded-2xl shadow-lg p-6 text-center">
            <div class="text-5xl mb-3">👥</div>
            <p class="text-3xl font-black text-green-600">8</p>
            <p class="text-sm mt-1">Messages reçus</p>
        </div>
    </div>

    <div class="text-center mt-10 space-x-4">
        <a href="{{ path('recruiter_offer_new') }}" class="bg-gradient-to-r from-purple-600 to-pink-600 text-white font-bold text-lg px-6 py-2 rounded-full">
            CRÉER UNE OFFRE
        </a>
        <a href="{{ path('joboffer_list') }}" class="bg-gradient-to-r from-green-500 to-blue-500 text-white font-bold text-lg px-6 py-2 rounded-full">
            TOUS LES OFFRES
        </a>
    </div>
</div>
{% endblock %}
